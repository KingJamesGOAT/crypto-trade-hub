# Environment Variables for Vercel Deployment

## Required Environment Variables

Add these in your Vercel project settings (Settings → Environment Variables):

### Authentication
```
JWT_SECRET=your-random-secret-key-here-change-this-in-production
ADMIN_USERNAME=blackswan
ADMIN_PASSWORD=Bl@ckSw4n_St3vE!92#Xq
```

### API Keys
```
GEMINI_API_KEY=AIzaSyDgmsl6BOL1PuZdzHHz5fIiahiU-oYT6RI
BINANCE_API_KEY=your-binance-api-key-here
BINANCE_SECRET_KEY=your-binance-secret-key-here
```

## How to Deploy

1. Push your code to GitHub
2. Import the repository in Vercel
3. Add the environment variables above in Vercel dashboard
4. Deploy!

## Security Notes

- **NEVER** commit `.env` files with real credentials to Git
- The JWT_SECRET should be a long random string (use: `openssl rand -base64 32`)
- API keys are only accessible to authenticated users via the backend
- All credentials are stored server-side in Vercel environment variables
